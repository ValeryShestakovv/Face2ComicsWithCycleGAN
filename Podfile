# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GAN_for_IOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'lottie-ios'
  pod 'SnapKit', '~> 5.6.0'
  pod 'AnimatedCollectionViewLayout'

  # Pods for GAN_for_IOS

  target 'GAN_for_IOSTests' do
    inherit! :search_paths
    pod 'SnapKit', '~> 5.6.0'
  end

  target 'GAN_for_IOSUITests' do
    # Pods for testing
  end

  target 'GAN_for_IOSUnitTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
